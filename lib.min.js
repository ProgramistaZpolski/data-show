"use strict";function dataShow(){document.querySelectorAll("[data-show]").forEach((elem=>{const result=eval(elem.dataset.show),display=elem.dataset.showmode??"block";return elem.style.display=result?display:"none",result}))}
